# CallDesk 1.0.1 发布版本设计

## 目标

将 CallDesk 的发布版本更新为 1.0.1，并把构建号从 1 递增到 2。

## 设计

仅修改 Xcode 工程的 `MARKETING_VERSION` 和 `CURRENT_PROJECT_VERSION`。应用目标与两个测试目标的 Debug、Release 配置统一使用 1.0.1（2），避免在构建、测试和设置页读取版本时出现不一致。

## 验证

先验证旧配置不满足目标值，再验证所有六组构建配置均为 1.0.1（2），并编译 iOS Simulator 构建。
