
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Local macOS metadata and Xcode build output
.DS_Store
/build/

# Visual brainstorming mockups (local only, not part of the app)
.superpowers/
